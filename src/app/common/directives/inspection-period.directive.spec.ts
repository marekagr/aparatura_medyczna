import { InspectionPeriodDirective } from './inspection-period.directive';

describe('InspectionPeriodDirective', () => {
  it('should create an instance', () => {
    const directive = new InspectionPeriodDirective();
    expect(directive).toBeTruthy();
  });
});
