import { GetAlertClassPipe } from './get-alert-class.pipe';

describe('GetAlertClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GetAlertClassPipe();
    expect(pipe).toBeTruthy();
  });
});
